// Operadores Aritmeticos e JavaScript
// Servem para realizar operações ,matematicas básicas

// Os mais comuns são:
// Adição (+)
let x = Number(prompt("Digite um numero:"));
let y = Number(prompt("Digite outro numero:"));

let soma = x + y;

alert(`O resultado da soma entre ${x} e ${y} é igual a: ${soma}`);
console.log(soma);

// Subtração 
letsubtracao = x - y;
comsole.log(subtracao);
alert(`o resultado da subtracao é: ${subtracao}`);

// Multiplicação (*)
let multiplicacao = x * y;
console.log("o resultado da multiplicacao é " + multiplicacao);

// Divisão ( / )
let divisao = x / y;
alert(`o resultado da divisao é: ${divisao}`);

// Exponenciação (**)
let exponenciacao = x ** y;
console.log(`o resultado da exponenciacao é: ${exponenciacao}`);

// Resto de divisão (%) - conhecido como participar de modulo. Geralmente retorna o resto de uma divisao  entre dois numeros 
let restoDivisao = x % y;
console.log(restoDivisao);

